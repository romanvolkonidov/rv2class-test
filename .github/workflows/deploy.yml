name: Auto Deploy to Vultr

on:
  # Deploy on every push to main
  push:
    branches: [ main ]
  
  # Also deploy every night at 2 AM UTC
  schedule:
    - cron: '0 2 * * *'
  
  # Allow manual deployment
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      
    - name: Deploy to Vultr Server
      env:
        SERVER_IP: ${{ secrets.SERVER_IP }}
        SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
      run: |
        echo "🚀 Deploying to production server..."
        
        # Debug: Check if secrets are set
        if [ -z "$SERVER_IP" ]; then
          echo "❌ ERROR: SERVER_IP secret is not set!"
          echo "Please add SERVER_IP=108.61.245.179 to GitHub Secrets"
          exit 1
        fi
        
        if [ -z "$SSH_PRIVATE_KEY" ]; then
          echo "❌ ERROR: SSH_PRIVATE_KEY secret is not set!"
          exit 1
        fi
        
        echo "✅ Secrets verified. Deploying to: $SERVER_IP"
        
        # Setup SSH key
        mkdir -p ~/.ssh
        echo "$SSH_PRIVATE_KEY" > ~/.ssh/deploy_key
        chmod 600 ~/.ssh/deploy_key
        
        # Add server to known hosts
        ssh-keyscan -H "$SERVER_IP" >> ~/.ssh/known_hosts 2>/dev/null
        
        # SSH and deploy using key authentication
        ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no "root@$SERVER_IP" << 'ENDSSH'
        set -e
        
        echo "📂 Navigating to app directory..."
        cd /var/www/rv2class
        
        echo "📥 Pulling latest code..."
        git fetch origin
        git reset --hard origin/main
        
        echo "📦 Installing dependencies..."
        npm install
        
        echo "� Setting up environment variables..."
        cat > .env.local << 'EOF'
        # Firebase Configuration
        NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSyB_VsLZaaQ_m3WNVlPjfhy715BXo8ax004
        NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=tracking-budget-app.firebaseapp.com
        NEXT_PUBLIC_FIREBASE_DATABASE_URL=https://tracking-budget-app-default-rtdb.firebaseio.com
        NEXT_PUBLIC_FIREBASE_PROJECT_ID=tracking-budget-app
        NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=tracking-budget-app.appspot.com
        NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=912992088190
        NEXT_PUBLIC_FIREBASE_APP_ID=1:912992088190:web:926c8826b3bc39e2eb282f
        
        # Jitsi Configuration
        NEXT_PUBLIC_JITSI_DOMAIN=app.rv2class.com
        EOF
        
        echo " Building Next.js app..."
        npm run build
        
        echo "📋 Deploying Jitsi config files..."
        if [ -f "jitsi-custom/jitsi-meet/config.js" ]; then
          echo "  → Copying config.js to /etc/jitsi/meet/"
          cp jitsi-custom/jitsi-meet/config.js /etc/jitsi/meet/app.rv2class.com-config.js
        fi
        
        if [ -f "jitsi-custom/jitsi-meet/interface_config.js" ]; then
          echo "  → Copying interface_config.js to /usr/share/jitsi-meet/"
          cp jitsi-custom/jitsi-meet/interface_config.js /usr/share/jitsi-meet/interface_config.js
        fi
        
        echo "♻️  Restarting all services..."
        echo "  → Restarting Jitsi services..."
        systemctl restart prosody jicofo jitsi-videobridge2 2>/dev/null || echo "  ⚠️  Jitsi services not found (skip if not installed)"
        
        echo "  → Reloading Nginx..."
        systemctl reload nginx
        
        echo "  → Restarting frontend..."
        pm2 restart rv2class || pm2 start ecosystem.config.js
        pm2 save
        
        echo "✅ Deployment complete!"
        echo ""
        echo "📊 Service Status:"
        pm2 list
        systemctl status prosody jicofo jitsi-videobridge2 --no-pager 2>/dev/null || echo "Jitsi not installed"
        ENDSSH
    
    - name: Deployment Status
      if: always()
      run: |
        if [ $? -eq 0 ]; then
          echo "✅ Deployment successful!"
        else
          echo "❌ Deployment failed!"
          exit 1
        fi
