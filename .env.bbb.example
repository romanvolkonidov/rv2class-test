# BigBlueButton Configuration
# Get these values from your BBB server by running: bbb-conf --secret

# BBB server URL (must end with /bigbluebutton/)
BBB_URL=https://your-bbb-server.com/bigbluebutton/

# BBB shared secret (from bbb-conf --secret)
BBB_SECRET=your_bbb_secret_here

# Optional: Set to true to enable BBB debug logging
BBB_DEBUG=false
